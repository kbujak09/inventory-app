extends layout 

block content 
  h1.title= title
    a.categoryPrevious(href=category.url) <
  hr
  if items.length 
    h2.deleteError Delete the following items before attempting to delete this category.

    div 
      h2 Items:

      ul.deleteList
        each item in items
          a.deleteItem(href=category.url + item.url)= item.name
  else
    h2 Do you really want to delete this Category?

    form(method='POST' action='')
      div.formGroup
        input#categoryid.formInput(type='hidden' name='categoryid' required='true' value=category._id) 
      
      button.formSubmit(type='submit') Delete
  